import React from 'react'
import { WeatherCollection } from 'services/WeatherData/WeatherData'

const WeatherWidget = (weatherData: WeatherCollection | undefined) => (
  <div>

  </div>
)

export default WeatherWidget
